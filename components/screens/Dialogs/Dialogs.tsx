import { ReactNode } from 'react'
import styles from './Dialogs.module.scss'
import DialogList from '@/components/ui/DialogList/DialogList'

const Dialogs = ():ReactNode => {
  return(
    <DialogList />
  )
}

export default Dialogs